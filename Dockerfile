# Використовуємо офіційний образ Golang як базовий
FROM golang:1.16

# Встановлюємо директорію роботи всередині контейнера Docker 
WORKDIR /go/src/app

# Копіюємо всі наші локальні файли .go до поточного каталогу всередині контейнера Docker
COPY . .

# Збираємо наш додаток
RUN go build -o main .

# Виконуємо програму 
CMD ["./main"]
